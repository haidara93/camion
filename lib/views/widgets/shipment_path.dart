import 'package:flutter/material.dart';

class ShipmentPath extends StatelessWidget {
  const ShipmentPath({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
